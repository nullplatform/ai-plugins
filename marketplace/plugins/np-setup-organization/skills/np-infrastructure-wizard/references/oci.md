# Decision Tree - OCI Infrastructure

> Invoked from step 4 of the main wizard (`SKILL.md`).

TODO: Implement following the pattern in [azure.md](azure.md).

Expected modules: vcn, oke, oci_dns, nat_gateway, cert_manager, istio, external_dns, prometheus.
